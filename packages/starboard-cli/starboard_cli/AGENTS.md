# starboard-cli

Command-line interface for interactive Databricks workload analysis.

## Key Modules
- `main.py` -- CLI entrypoint and argument parsing
- `cli/` -- Subcommand implementations and CLI framework
- `sessions/` -- Session management for interactive analysis
