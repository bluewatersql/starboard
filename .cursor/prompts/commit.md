# Workspace Git Sync

- Commit everthing - no untracked, uncommitted changes
- Merged to Main - all changes are merged into main
- Branches are cleaned up - Temporary/feature branches are deleted locally and remotely when no longer needed
- Synced to remote - main is up to date with remote (pull) and all local commits are pushed.

- If the committed changes, add to, update or change the guidance in Claude.md or the .cursor/ folder. Update the project guidance.