# Tests

## Organization
- `tests/architecture/` — Architecture fitness tests (GUIDELINE-001–010)
- `tests/contract/` — API contract tests
- `tests/integration/` — Cross-package integration tests
- `tests/golden/` — Golden/snapshot tests for prompts
- `packages/*/tests/unit/` — Per-package unit tests
